---
name: Unit Test

on: [ push ]

jobs:
  runs:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Build java app
        uses: ./.github/actions/build

      - name: Run tests
        run: mvn test
